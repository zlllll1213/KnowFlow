# KnowFlow Demo Data

Use `scripts/seed-demo-data.sh` after backend, Go RAG, Worker, PostgreSQL, Redis, and MinIO are running.

The script creates a demo account, knowledge base, and uploads a small Markdown document about KnowFlow architecture.
